config:
  datasource:
    host: lab3-postgresql
    user: myuser
    # Only one of password and passwordSecret can be specified
    password: passwd
    # Specify passwordSecret if DB password is stored in secret.
    passwordSecret: {}
    # Secret name
    #  name:
    # Password key inside secret
    #  key:
    port: "5432"
    database: myapp
    sslmode: disable
  disableDefaultMetrics: false
  queries: |-